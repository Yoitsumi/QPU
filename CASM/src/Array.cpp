#include "..\include\Array.h"

namespace casm {
	/*
	template<typename T, bool checkBounds>
	Array<T, checkBounds>::Array(unsigned l) :
		ptr(new T[l]),
		length(l)
	{
	}


	template<typename T, bool checkBounds>
	Array<T, checkBounds>::~Array() {
		delete[] ptr;
	}
	*/



}